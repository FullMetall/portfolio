import "@fontsource-variable/golos-text";
import "@fontsource-variable/oswald";
import "../../concepts/concepts.css";

export default function LiftAutomationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
