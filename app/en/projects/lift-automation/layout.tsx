import "@fontsource-variable/golos-text";
import "@fontsource-variable/oswald";
import "../../../portfolio.css";

export default function EnglishLiftAutomationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
